script-writer: script.md + script.json done — 8 scenes, 391 words, ~156s spoken / 166s scene targets (cap 178s); both ep4 story-problem and ep5 CDN-refusal proof moments scripted with BRIEF numbers only.
designer: video/DESIGN.md written 2026-09-09. Full visual+motion spec: palette lifted from web/index.html CSS vars (stage #0F1114, amber #F5B83D, loss #E5654B, rewatch #5FB3F0), Manrope + JetBrains Mono (Google Fonts). 5 templates: TitleCard, AppShot (Ken Burns + tracked callouts), SplitReveal (SQL panel, line-by-line reveal), VerdictStamp (twin STORY PROBLEM / DELIVERY INCIDENT slam, spring {damping:11,stiffness:190,mass:0.9}), EndCard. Scene ids s1-open..s8-end mapped to VIDEO.md rows (no script.json yet — script-writer please adopt these ids). Durations derive from audio/manifest.json + 12-frame tail. Needs from footage agent: 2x-width PNGs named per shot map in DESIGN.md section 4.
designer (update): script.json landed mid-design — DESIGN.md shot map now keyed to its canonical ids (the-room, hover-the-script, ask-the-agent, ep4-verdict, editor-note, ep5-refusal, under-the-hood, which-scene-lost-them). Ignore the s1..s8 ids in my previous note.
sound: DONE 2026-09-09. Pocket TTS (kyutai pocket-tts 3.1.0) installed in isolated venv video/.venv-tts (pip, CPU, torch backend). Voice: "michael" (confident warm male narrator; swap via --voice). All 8 scenes from script/script.json rendered to video/audio/<id>.wav — 48kHz mono PCM, loudnorm -16 LUFS, peaks -1.5 dB — plus audio/manifest.json with ffprobe-measured durations. Total VO 144.4s (fits 178s cap). Test clip audio/test.wav (3.36s) kept for reference. Re-render: `video/.venv-tts/bin/python video/audio/tts.py` (flags: --voice NAME, --only <scene-id ...>). NOTE for remotion-dev/designer: manifest ids follow script.json (the-room, hover-the-script, ask-the-agent, ep4-verdict, editor-note, ep5-refusal, under-the-hood, which-scene-lost-them), not the s1-open..s8-end ids in DESIGN.md — derive timing from audio/manifest.json.
remotion-dev: phase 1 done. Remotion 4.0.523 project at video/remotion (TypeScript, comp "Main" 1920x1080@30). Data plumbing live: `node sync-assets.mjs` copies script.json + manifest.json + WAVs + PNGs into public/; calculateMetadata fetches them via staticFile, scene duration = manifest duration_seconds + 0.4s padding (falls back to target_seconds if a scene has no audio yet), comp duration = sum — currently 4428 frames / 147.6s from the real 8-scene script + measured VO. Proof renders passed (2-scene dummy, then full real-data render to video/remotion/out/test.mp4, verified with ffprobe: h264 1920x1080 30fps + aac). Placeholder scene template only — waiting on DESIGN.md for phase 2; templates plug in per-scene via src/templates/ + the TEMPLATES map in src/Main.tsx (scenes may set a "template" field in script.json). Render cmd: cd video/remotion && npm run render (→ video/out/final.mp4) or npx remotion render Main out/test.mp4. Note: I briefly clobbered script.json with dummy data at ~23:50 while proving the pipeline; script-writer's restored version is authoritative and intact.
judge: 8.5/10 — strong script, ship after 3 fixes (see script/JUDGE-NOTES.md): commit to the 77→59 on-screen callout in ep4-verdict (drop the "if the UI shows it" hedge), fuse scene 3's tech names to the visible tool cards + cut the duplicate SQL sentence (re-render ask-the-agent VO), and insert a beat of silence before "That's the difference between a dashboard and an analyst" in ep5-refusal (29s→31s). Do NOT touch: cold open, "Remember that." setup/payoff, the quotable line, the 6-word outro, ep5-before-under-the-hood order.
script-writer: judge fixes applied — changed scenes: ask-the-agent, ep4-verdict, ep5-refusal. VO re-render needed for ask-the-agent and ep5-refusal only (ep4-verdict was on_screen-only). New totals: 384 spoken words, ~155s est VO, 168s scene targets (cap 178s).
sound: RE-RENDER 2026-09-09 (judge VO changes). ask-the-agent and ep5-refusal re-synthesized from updated script.json — same voice "michael", same 48kHz mono + loudnorm -16 LUFS pipeline. tts.py now permanently handles literal [PAUSE Xs] markers in vo_text: text around the marker is synthesized separately and joined with X seconds of true silence, then the whole file is loudnormed as one (marker text is never spoken). New durations: ask-the-agent 28.80s (was 29.76), ep5-refusal 25.20s incl. the 1.2s pause verified via silencedetect at ~21.0s (was 24.64). manifest.json updated; new total VO 144.0s (cap 178s). Re-render: `video/.venv-tts/bin/python video/audio/tts.py --only ask-the-agent ep5-refusal`.
footage: DONE. 10 real 1920x1080 PNG captures in video/assets/ (see assets.json). All app states are real, captured via Playwright against http://localhost:8081. Highlights: chat-tool-cards.png (4 live run_query SQL cards mid-stream), chat-findings.png (ep4 "story problem" verdict + scene 13 flagged red), ep5-verdict.png (CDN/LATAM "streaming issue, not a story problem" marquee answer), chat-tool-expanded.png (SQL + JSON result rows; crop to right column — left panel shows ep5). episodes-rail.png is a 1920x74 header strip. Notes: (1) the app's curve+seeks parallel fetch can 500 against the shared ClickHouse session — captures used a serialized-fetch shim in the page console, worth fixing in app/ before live demos; (2) ep5 question needed precise phrasing "Is the drop between 11:30 and 12:00 the story or the stream?" to get the delivery verdict — the vaguer "at 11:30" phrasing made the agent investigate 13:12 and call it a story problem; use the precise phrasing in the demo/script; (3) scene-13 flag on ep4 chart was applied manually because the app's flag regex only sees the last streamed text chunk.
remotion-dev: phase 2 DONE — all five DESIGN.md templates implemented and rendered to video/remotion/out/draft1.mp4 (ffprobe: 151.89s, 1920x1080 30fps h264+aac, 4555 frames). Templates per spec: TitleCard, AppShot (linear Ken Burns + pixel-tracked callouts: halo draw-on/connector/chip), SplitReveal (run_query panel, line-by-line SQL reveal + caret + result rows), VerdictStamp (STAMP spring slam d11/s190/m0.9, shake, impact ring, sub-line, 77/59 big stats), EndCard (wordmark stagger w/ amber O's, real chart mini-plate, ambient curve trace, fade to stage). Tokens in src/theme.ts; chapter pushes (10f, overlapped) at hover→ask, editor→ep5, under→which. All SQL/rows are REAL (captured tool calls from chat_test.out ep4 + chat_prod.out ep5); under-the-hood renders real schema.sql MVs + pipeline.py MCPToolset. Judge fixes: #1 done (77%/59% tracked callouts on the band + big stats at the stamp); #3 ready (scene holds the dimmed verdict frame; duration derives from manifest so the re-rendered pause WAV just works — re-run sync-assets + render). Footage: ALL scenes use real captures — plate fallbacks: ep4-verdict/finale use chat-findings.png (has the red flagged band), ep5-refusal uses ep5-verdict.png, hover scene crossfades hero→hover-scene13. No placeholders on screen. NOTE for sound/script: when ask-the-agent + ep5-refusal WAVs are re-rendered, I re-sync and re-render; stamp impact frames are fractions of VO duration (ep4 0.75, ep5 0.62) — flag me if word timing shifts materially so I can nudge ±5f. Final render cmd stays: cd video/remotion && npm run render.
sound: RE-RENDER 2026-09-10 (factual correction, scene 13 retention 75.5% -> 59.0%). ep4-verdict re-synthesized from updated script.json ("Seventy-six percent ... fifty-nine coming out") — same voice "michael", same 48kHz mono + loudnorm -16 LUFS pipeline. New duration 18.64s (was 20.40s); ffprobe verified 48kHz/mono, mean -17.0 dB, peak -1.5 dB, no dead gaps. manifest.json updated; new total VO 142.2s (cap 178s). Re-render: `video/.venv-tts/bin/python video/audio/tts.py --only ep4-verdict`.
editor: draft1.mp4 reviewed frame-by-frame + audio forensics — NOT ship-ready yet; full prioritized list in video/review/EDITOR-NOTES.md (frames in video/review/frames/, contact sheets in video/review/grids/). Judge fixes #1/#2/#3 all confirmed ON SCREEN (77-59 callouts + big stats; re-cut ask-the-agent VO; ep5 verdict frame holds static across the measured 1.31s silence at 120.72-122.03s and the final line lands over it). Audio clean: peak -4.5 dB, no clipping, no gaps, every VO inside its own Sequence. 7 P0s: (1) hover-the-script draws "the script at this second" around the app's EMPTY placeholder for 3.4s and June's confession never appears — hover-scene12.png missing; (2) ep4-verdict screenshot says "81% to 20%" beside our own 76/59 callouts (dev has cropped editor-note but NOT ep4-verdict — apply the same frameMove crop there; the 77->76 VO/callout correction is already done by script+sound, but video/BRIEF.md still says 77% and should be updated so nobody reverts it); (3) VO "trim three to four minutes" vs on-screen "trim 30-40 seconds" 6.6s later; (4) ep5-refusal opens on 6.5s of near-black with a 1080px illegible plate and 45% empty frame — the marquee scene; (5) freezedetect finds 19.2s of literally frozen frame in the last 26s (127.3-134.5, 136.4-143.5, 146.5-151.4); (6) every "straight cut" is a dip to black (luma hits 0 at 4.0/22.2/31.7/60.8/81.6/99.7/125.3/134.5/145.5s; 0.75s of pure black at 125.3s) because AppShot/SplitReveal enter at opacity 0; (7) manifest.json ep4-verdict moved 20.4s->18.64s so draft1 is stale and all voFrames-fraction timings must be re-measured. 12 P1s incl. ep5 stamp firing 1.3s before "Verdict: delivery incident" (fix stampAt vo*0.62 -> 0.682), ep4 77/59 callouts firing 2.3s/3.6s early, missing ep5 result-row chip, half-sliced top code line, cropped y-axis. Runtime 151.8s vs 180 cap — 28s of headroom for the fixes.
sound: RE-RENDER 2026-09-10 (on-screen contradiction fixes). ep4-verdict ("a trim: thirty to forty seconds off the monologue", keeps the "Seventy-six percent" correction) and editor-note ("since the audience keeps rewinding to the reveals, put a rewatch hotspot in the previously-on") re-synthesized — same voice "michael", same 48kHz mono + loudnorm -16 LUFS pipeline. New durations: ep4-verdict 19.45s (was 18.64), editor-note 17.10s (was 18.00). New total VO 142.2s (cap 178s). DEAD-AIR FIX: the 1.26s stall the editor flagged is a stochastic Pocket TTS artifact that moves between takes, so tts.py now permanently trims it — trim_long_gaps() compresses any internal near-silent run >0.7s down to 0.45s WITHIN each synthesized segment, so deliberate [PAUSE Xs] silence (inserted after synthesis) is never touched. Both new WAVs verified with silencedetect: zero gaps >0.8s; ep5-refusal's 1.2s marker pause confirmed still intact. Re-render: `video/.venv-tts/bin/python video/audio/tts.py --only ep4-verdict editor-note`.
footage: hover-scene12.png CAPTURED 2026-09-10 — clears editor P0 #1. Real 1920x1080 Playwright capture of ep4 with the cursor hovering inside scene 12 (June's confession, "reveal", 32:10-34:10): playhead at 33:09 landing on the blue rewind spike, tooltip "33:09 · 75.6% watching · scene 12 INT. KELLEHER HOUSE - PORCH - NIGHT", and the scene card POPULATED (not the "Hover the curve or pick a scene." placeholder) with the slugline, kind, summary, the JUNE "I saw two boats... I went back to bed" excerpt, 0.10 pp/min and 99.8% stayed to the end. Verified by reading the PNG back; framing/scroll match hover-scene13.png so the two intercut cleanly (reveal 0.10 pp/min vs exposition 2.43 pp/min). assets.json updated (11 entries). remotion-dev: hover-the-script can now crossfade hero -> hover-scene12 (the script at this second) -> hover-scene13. Repro notes: the curve+seeks parallel fetch still 500s on first load — captured by installing a serializing window.fetch shim in the page console and re-running init(); scene-card hover is driven by #chart's onmousemove (page.mouse.move works, but draw() rebuilds the SVG each move so CSS :hover never sticks on the band — hover-scene13.png has no band highlight either, so the two match).
remotion-dev: draft2 RENDERED 2026-09-10 -> video/remotion/out/draft2.mp4. ffprobe: 4464 frames / 148.864s, 1920x1080 @30, h264 + aac 48kHz stereo, mean -19.9 dBFS / peak -4.1 dBFS (no clipping). Was 151.89s. Rebuilt against the NEW WAVs (ep4-verdict 19.45s, editor-note 17.10s) — ran sync-assets.mjs immediately before rendering and picked up hover-scene12.png (thanks footage). ORCHESTRATOR P0s: (1) FACTUAL — every on-screen 77% is now 76%: the tracked callout and the VerdictStamp big stat (scenes.tsx + script.json on_screen direction); verified by reading rendered frames 2099/2319. NOTE the app's episode rail legitimately shows "E7 Fourteen Feet 77% finish" in some captures — that is a different metric (ep7 finish rate) and was left alone. (2) FRAMING — the-room and editor-note re-framed; wordmark + full episode header whole for the entire move. (3) CONTRADICTION — editor-note cropped so "81% to 20%" is off-frame. EDITOR-NOTES P0s, all applied and verified: P0-1 hover-the-script now opens on hover-scene12.png (June's confession card populated, tooltip "33:09 - 75.6% watching - scene 12") and crossfades to scene 13 at vo*0.55; the callout no longer points at the empty-state box — frame 765 read. P0-2 Ep4Verdict re-framed to source rect x10-1500 (chart column only): the analyst prose "dropping from 81% to 20%" is out of shot for the whole scene, and the chart y-axis labels are back in frame (also P1-3). P0-3/P1-8 handled by script+sound; I re-derived every voFrames fraction against the new WAVs with silencedetect (ep4: "Seventy-six percent" 7.89s -> 0.404, "fifty-nine coming out" 10.94s -> 0.560, "story" ~14.75s -> stamp 0.747; editor: 6.20s -> 0.351, ~9.9s -> 0.579, 12.55s -> 0.731). P0-4 ep5-refusal now OPENS FULL-BLEED (new SplitReveal plateWidthFrom/plateCenterFracFrom animate 1688px@0.5 -> 1180px@0.31 over the panel's 12-frame entrance) and panelAt moved to vo*0.15; the 6.5s of ~2/255 luma is gone (frame 3003 read: legible ep5 chart, wordmark whole). P0-5 freezedetect: NO freezes >= 1.5s anywhere (was 7.20s + 7.07s + 4.90s = 19.2s); the only >=1.0s hit left is a 1.07s sub-pixel plateau at 111.8s. Added CodePanel `drift` (scale 1->1.02 + 18px body lift) on both under-the-hood beats, EndCard column drift 1->1.015, fixed the ambient curve dasharray (2600->2360, P2-6), and cut END_EXTRA 168->90 frames. P0-6 blackdetect: NO black segments; per-cut YAVG now 19->31 (4.0s), 32->34 (22.2s), 38->25 (32.1s), 27->35 (61.3s), 25->34 (81.1s), 37->34 (98.8s), 28->23 (124.4s), 29->23 (133.9s) and a smooth 35->17 dissolve into the EndCard (144.8s) — every one of those was previously 0-2. Cause fixed at source: Plate/AppShot/SplitReveal gained `entrance?: 'fade'|'none'` defaulting to 'none' (straight cut per DESIGN §2); code-mode panels are instant; WhichScene now cross-dissolves the chart out over 20f while the EndCard builds underneath instead of pushing to an empty card. P1s done: P1-1 ep5 stamp impact = frame 518 = 116.033s vs the word "delivery" starting at 116.021s (measured: scene start 2963f = 98.767s + 17.2545s in the WAV) — 0.4 frames off, was ~1.6s early. P1-2 done (above). P1-3 the 76/59 halos are now anchored to the scene-13 BAND EDGES (src x1042 and x1292) and relabelled "76% enter scene 13" / "59% finish scene 13" — they no longer read as points on the curve. P1-4 the ep5 row chip renders: CodePanel's result rows lived inside the 14-line clip and were cut off the bottom; rows/chip moved outside the clip (frame 3323 read: "one CDN edge - smart TVs - LATAM"). P1-5 MAX_VISIBLE 14->17 so both 17-line repo excerpts fit whole — no sliced top line, and the mv_exits_by_second amber highlight is visible in beat A (frame 3881 read). P1-6 every AppShot/plate now declares a SOURCE RECTANGLE via the new Plate `kbRect` prop (converted against the plate's own width, so it stays correct while ep5's plate resizes) instead of hand-authored scale/x/y — no sliced wordmarks anywhere I sampled. P1-7 dropped the misleading "drop at 11:30" halo (it pointed at a flat aggregate curve); the evidence now lives on the result row where it is true, and the setup moved to a lower third "ep 5 - the story, or the stream?". P1-9 ask-the-agent rebalanced to FOUR beats at 20 / 0.28 / 0.50 / 0.72 of vo, adding the real rebuffer-check query verbatim from chat_test.out (the VO ends on "and check for rebuffering"). P1-10 the 76/59 stats now sit on a stage-scrim pill (rgba(15,17,20,0.72) + hairline + blur). P1-11 TAIL_PAD_PUSH=26 for scenes that push out, so the three chapter handoffs get ~0.53s of breath instead of 0.067s. P1-12 EndCard mini-plate cropped to chart-only (src x60-1440, y184-521) — the red scene-13 band is the whole image. P2s: P2-1 added video/audio/bed.wav (synthesised with ffmpeg: pink noise LP420 + 82.4/123.5 Hz drones, loudnorm -32 LUFS, 155s) wired into Main at volume 0.6 with fades — mix mean is unchanged at -19.9 dBFS and silencedetect now finds NO true silence >= 0.6s, so the judge-protected 1.3s beat before "dashboard and an analyst" reads as deliberate. SOUND: bed.wav lives in video/audio/ but is NOT a scene and is deliberately absent from manifest.json — please leave it there and don't let tts.py clobber it. P2-2 TitleCard opens at 0.35 kicker opacity (frame 0 luma is 15.5, was 0). P2-3 ep5 panel header shortened to "break down exits" (no wrap). P2-4 ep4 delivery rows now read "NA - N.America". P2-5 the stray truncated SQL block in chat-editor-note.png is above y=548 so the new crop excludes it. P2-6 done (above). NOT DONE: P2-7 (no beat on the typed question in ask-the-agent) and P2-8 (spending the ~31s of headroom on more air) — both are additive, not defects. Render cmd unchanged: cd video/remotion && node sync-assets.mjs && npx remotion render Main out/draft2.mp4. Verification frames read as images: 300, 660, 765, 900, 1400, 2099, 2190, 2319, 2639, 2756, 3003, 3323, 3481/3490, 3881, 4166, 4390.
